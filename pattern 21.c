#include<stdio.h>

int main()
{
    int i, j, n, x=1, cnt=1;
    scanf("%d",&n);
    for(i=1; i<=n; i++){
            printf("%d",x);
        for(j=x; cnt<=n; j++){
            printf("%d")
        }
    }
    return 0;
}
